package com.yuva.leetcode.general;

import java.util.HashMap;
import java.util.Map;

public class LongestSubstringKRepeat {

	/*
	 * public int longestSubstring(String s, int k) { Map<Character, int[]> charMap
	 * = new HashMap<>(); char[] charArr = s.toCharArray();
	 * 
	 * for (int i=0; i < charArr.length; i++) { if
	 * (!charMap.containsKey(charArr[i])) { int []countArr = new
	 * int[charArr.length]; countArr[i] = 1; charMap.put(charArr[i], countArr); }
	 * else { int countArr[]= charMap.get(charArr[i]); countArr[i] = countArr } } }
	 */
}
